# Changelog

| Date | Change |
|------|--------|
| 2026-07-20 | Expanded investment-ops customization and interaction: Watchlist now supports ordered visible columns, richer quote fields, instrument metadata, and optional controlled/uncontrolled accordion detail rows; options templates use consistent call/put, buy/sell, status, and profit/loss treatments; PayoffDiagram, PayoffBundleCard, and OptionPositionCard add opt-in tracked hover dots and price/P&L tooltips. |
| 2026-07-20 | Shipped Phase J-open: options-contract layer for `@agentix/ui`. Added options domain types to `lib/finance-types` (`Greeks`, `OptionType`, `OptionContract`, `OptionPosition`, `OptionPositionStatus`, `OptionChainRow`, `OptionLeg`, `PayoffPoint`) + pure helpers in new `lib/finance-utils` (`computePayoffAtExpiry`, `breakevensAtExpiry`, `maxProfit/LossAtExpiry`, `netPremium`, `priceGrid`). Added **10 new components** under `src/components/`: `OptionSymbolBadge` (ROOT MM/DD/YY STRIKE{C\|P} token composing `Badge`), `ExpiryBadge` (color-graded DTE pill), `GreeksDisplay` (Δ/Γ/Θ/ν/ρ cluster with glossary tooltips), `BreakevenBadges` (breakeven pill row), `PayoffDiagram` (inline-SVG hockey-stick payoff curve — `Sparkline` precedent), `GreeksDecayChart` (inline-SVG Greek decay mini chart), `IVChart` (inline-SVG IV term structure + small IV surface heatmap), `StrikesNavigator` (windowed strike ladder with ITM/ATM/OTM striping), `LegBuilderRow` (Buy/Sell × Call/Put × strike × qty row), `SpreadTypeSelector` (spread template picker). Added **6 new templates** under `src/templates/investment-ops/`: `OptionsPositionsTable` (open-positions blotter mirroring `HoldingsTable`), `AggregateGreeksStrip` (net portfolio Greeks strip), `OptionPositionCard` (single-position summary), `OptionsChain` (calls/puts chain by strike × expiry with `StrikesNavigator`), `PayoffBundleCard` (multi-leg risk snapshot), `MultiLegOrderTicket` (`Sheet` + `SpreadTypeSelector` + `LegBuilderRow` list + `PayoffBundleCard` preview + `AlertDialog` + `toast.promise`). New types + helpers re-exported through `./` entry, `./finance` secondary entrypoint, and `./templates/investment-ops`. No new deps; chart geometry stays in `@agentix/charts` (deferred). Verified `npx tsc --noEmit` clean, `npm run build` emits ESM+CJS+DTS for all 10 components + 6 templates, `npm run build-storybook` exits 0. Stories live at `Components/*` (10 primitives, PascalCase) and `Templates/Options/*` (6 templates, kebab-case). ROADMAP subfiles updated: `advanced-components.md`, `templates.md`, `changelog.md`. |
| 2026-07-20 | Added Storybook stories for the 8 investment-ops templates that were missing them: `StatTile`, `AccountSummary`, `Watchlist`, `HoldingsTable`, `OrderTicket`, `AllocationBreakdown`, `NewsFeed`, and `InvestmentOpsDashboard`. Stories live alongside their components under `src/templates/investment-ops/*.stories.tsx` and are grouped under the new `Templates/Investment-Ops/*` section in Storybook. `InvestmentOpsDashboard` uses `layout: "fullscreen"`; `OrderTicket` stories wrap render in `PremiumToastProvider`. Verified `npm run build-storybook` exits 0. |
| 2026-07-19 | Phase A bugfixes — Resolved 2 blockers, 6 majors, and 13 minor issues from `docs/bugfix-handoff-phase-a.md`. Blockers: `Sparkline` `useGradientId` rules-of-hooks violation (conditional `useId()`); `VisuallyHidden` legacy `clip-[rect(...)]` / `focus:clip-auto` utilities produced no CSS — switched to `sr-only` / `focus:not-sr-only`. Majors: `Label` `peer-disabled:*` now active after adding `peer` to `Input` base classes; `EmptyState` spacing restored to spec (outer `gap-4`, cluster `gap-1`, dropped `mt-2` wrapper); `VisuallyHidden` polymorphic ref documented (`HTMLElement` cast note); `Sparkline` & `MiniBars` inline `width`/`height` no longer override Tailwind `w-*`/`h-*` classes (SVG sized via viewBox + classes); `SegmentedProgress` `max < sum` now warns in dev and palette is reactive (computed in-component via `useMemo` + `MutationObserver` on theme). Minors: `TrendIndicator` signed prefix keyed on value sign (not direction) + neutral color aligned with `NumericText`; `MiniBars` partial palette doc + bar offset clamp; `Gauge` needle label now tweens with the value, trailing threshold segment falls back to `trackColor`, and `describeArc` inverted naming is documented; `SegmentedProgress` empty-state no longer doubles muted divs and switched `role="meter"` → `role="presentation"` with `aria-label`; `Sparkline` gradient uses `userSpaceOnUse`, `variant="bar"` negative-value behavior documented, and Storybook `variant` argType fixed; `EmptyState` added `icon={false}` story; `FieldLabel` dropped redundant `cn()`. Verification: `npx tsc --noEmit` clean, `npm run build` exits 0, `npm run build-storybook` exits 0. Deferred (out-of-scope): `tsconfig.json` `*.stories.tsx` exclusion (workspace-level), full generic `VisuallyHidden` ref refactor, `EmptyState` configurable `headingLevel`, `EmptyState` custom-icon muted color wrapping. |
| 2026-07-19 | Shipped Phase A of the finance/quant UI primitives (`docs/finance-quant-component-roadmap.md`). Added 10 new components to `@agentix/ui` with no new dependencies: `TrendIndicator` (signed value + arrow using existing `pnlColorClass`), `Kbd` (inline `<kbd>`), `VisuallyHidden` (sr-only a11y primitive), `Label` (plain `<label>` per shadcn — `@radix-ui/react-label` not added), `Field` (`Field`/`FieldLabel`/`FieldDescription`/`FieldError`/`FieldContent` flat-composition form wrapper), `Sparkline` (dependency-free inline SVG line/area/bar micro-chart, drop-in for `StatTile` sparklineRenderSlot), `MiniBars` (one-row divergent SVG histogram), `Gauge` (semicircle/full radial dial with thresholds, sibling of `Progress`), `SegmentedProgress` (horizontal/vertical range bar with multi-segment value/color), `EmptyState` (zero-state composer composing muted icon + title + description + CTA). Confirmed `Typography` + `NumericText` already implemented; they were the previously-listed P0s from `§2`. Verified `npx tsc --noEmit` clean, `npm run build` produces ESM+CJS+DTS, `npm run build-storybook` exits 0. Documented architecture decision splitting charting into a future `@agentix/charts` package (`docs/chart-library-strategy.md`) and updated `docs/finance-quant-component-roadmap.md` with package-destination columns and a 10-phase build order. ROADMAP index, `docs/ROADMAP.md`, and README updated with strategy + roadmap references. |
| 2026-07-16 | Investment-ops dashboard set shipped across 6 phases. Phase 1 added finance utilities (`number-utils`, `time-utils`, `color-utils`, `finance-types`), semantic `--positive`/`--negative` tokens in `globals.css` + Tailwind, and numeric primitives (`Typography`+`NumericText`, `AnimatedNumber`, `ToggleGroup`). Phase 2 added `Resizable` wrapping `react-resizable-panels` v3 (`Group`/`Panel`/`Separator` + `useDefaultLayout`) with `autoSaveId` persistence and SSR guards. Phase 3 added `CommandPalette` (cmdk + Dialog + ScrollArea) and `Toolbar` (Radix Toolbar + composition of `ToggleGroup`). Phase 4 added finance streaming widgets `OrderBook` and `TimeAndSales` (memoized rows, throttled SR summaries, autoscroll). Phase 5 composed 7 investment-ops template blocks under `src/templates/investment-ops/` plus the `InvestmentOpsDashboard` shell. Phase 6 added secondary entrypoints `./finance` and `./templates/investment-ops` to `tsup.config.ts` and `package.json` `exports`. New deps: `@radix-ui/react-toggle-group`, `@radix-ui/react-toolbar`, `react-resizable-panels`. Updated roadmap subfiles (advanced-components, templates, phases, changelog). |
| 2026-06-26 | Publish readiness fixes - Corrected `exports` map so the `types` condition resolves first (fixes TS types for `moduleResolution: bundler/node16`). Added `repository`, `bugs`, `homepage`, and `publishConfig: { access: "public" }` metadata to `package.json`. Added `./tailwind.config` subpath export. Removed unused `sonner` and `react-hot-toast` runtime dependencies (neither imported in `src/`). Pruned unused `addHours` re-export from `date-utils.ts`. Verified `tsup` build succeeds and `npm pack --dry-run` ships only `dist/`, `tailwind.config.js`, `LICENSE`, `README.md`, `package.json`. |
| 2026-04-03 | Single-tier refactor - Flattened component folders into `src/components/<component>`, removed free/premium directory splits, neutralized main exports around duplicated component families, and renamed roadmap sections/files to reflect a unified library structure. |
| 2026-03-23 | Enhanced `MultiSelect` selected-value chips with configurable `chipVariant` and `chipColor` props. Added semantic chip colors to the shared `Chip` component so selected values can be styled as assist, filter, input, or suggestion chips across default, primary, secondary, success, warning, and destructive treatments. |
| 2026-03-23 | Completed foundational table work and data grid support. Added `DropdownMenu` and `Table` primitives, then built `DataTable` with sorting, toolbar filtering, column visibility, row actions, pagination, row selection, and virtualization via TanStack Table + React Virtual. Updated exports, stories, and roadmap status across the roadmap files. |
| 2026-03-16 | Roadmap verification audit - Confirmed all components match actual implementation status. Removed erroneous planned entries for Context Menu and Hover Card, both of which were already implemented. Verified exports including `InlineTimePicker`, `MultiSelectGroup`, `MultiSelectSeparator`, infinite scroll support in `ScrollArea`, and the richer `Slider` feature set. |
| 2026-01-06 | Roadmap audit update - Discovered 2 new components implemented since last audit: Context Menu and Multi-Select. Context Menu provides right-click action menus with Radix UI, keyboard shortcuts, submenus, checkbox/radio items. Multi-Select enables multi-selection with tag display, search filtering, virtualization, infinite scroll, and nested grouping. Updated roadmap counts and implementation status across the roadmap files. |
| 2026-01-05 | Roadmap audit and update - Verified actual implementation status across all components and corrected inflated counts in the roadmap. Confirmed additions including Chip, InputIncrementor, Navigation, Pagination, Progress, Stepper, Toast, BasicSlider, Breadcrumb, Skeleton, Separator, FAB, advanced Toast flows, Carousel, date/time pickers, and navigation submenus. |
| 2025-01-09 | Completed Date/Time Picker components - `DatePicker` (single/range/multiple selection modes, min/max constraints, disabled dates/days of week, presets, `Calendar`/`YearSelector`/`Presets` sub-components), `TimePicker` + `InlineTimePicker` (12h/24h formats, scrollable selectors, minute steps, AM/PM toggle), `DateTimePicker` (combined date+time, tabs/side-by-side layouts). Added date-utils library with formatting, parsing, validation functions. Dependencies: date-fns, react-day-picker |
| 2025-12-29 | Completed Navigation submenus with `NavItemWithSubmenu` (accordion-based expansion, animated chevrons, custom icons, nested submenu support, controlled/uncontrolled state, level-based indentation) and `SubmenuItem` (compact 36px height, reduced padding, simplified variants for visual hierarchy). Integrated with `NavigationProvider` context. |
| 2025-01-XX | Refactored Slider into a basic API (xs/sm/md sizes, bar handle, press animation, gap styling, range support) plus an expanded API with lg/xl sizes, circular/knobless handles, value indicators, stop indicators, and inset icons. |
| 2025-12-23 | Completed Carousel with `Carousel`, `CarouselContent`, `CarouselItem`, `CarouselPrevious`, `CarouselNext`, `CarouselIndicators`, `CarouselProgress`, and `CarouselCounter`. Added slide/fade/zoom/flip transitions, responsive layouts via basis classes, dot/line/number indicators, and embla autoplay support. |
| 2025-12-19 | Completed InputIncrementor component - Numeric input with +/- buttons, 3 variants (default/embedded/minimal), 3 sizes, min/max/step constraints, decimal precision, custom formatters, keyboard navigation, ARIA spinbutton pattern |
| 2025-12-18 | Completed Navigation component - Full navigation system with `NavigationProvider`, `Navbar` (5 variants: default/prominent/dense/centered/split), `Navrail` (5 variants: standard/compact/mini/floating/inset), `Navdrawer` (slide-in drawer with sizes), shared components (`NavItem`, `NavSection`, `NavDivider`, `NavBadge`), `useNavigation()` hook, responsive behavior |
| 2025-01-XX | Completed Pagination component - Compound component pattern with `Pagination`, `PaginationContent`, `PaginationItem`, `PaginationLink`, `PaginationPrevious`, `PaginationNext`, `PaginationFirst`, `PaginationLast`, `PaginationEllipsis`, `PaginationPageSizeSelector`, `ResponsivePagination`, `usePaginationRange()` hook |
| 2025-01-XX | Completed Breadcrumb component - Compound component pattern with `Breadcrumb`, `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbPage`, `BreadcrumbSeparator`, `BreadcrumbEllipsis`, `ResponsiveBreadcrumb`, `useResponsiveBreadcrumb` hook |
| 2025-12-16 | Completed advanced Stepper interactions with vertical orientation, Radix Collapsible content, non-linear navigation, async validation, custom connectors (solid/dashed/gradient), and branching flows support. |
| 2025-12-15 | Completed Stepper (basic) component - Compound component pattern with `Stepper`, `StepperList`, `StepperItem`, `StepperTrigger`, `StepperIndicator`, `StepperLabel`, `StepperSeparator`, `StepperContent`, `useStepperNavigation()` hook |
| 2025-12-09 | Completed Toast component - Store/event emitter pattern with imperative `toast()` API, `useToast()` hook, 5 variants, 6 positions, Avatar/Button integration |
| 2025-12-08 | Completed Sheet/Drawer APIs with base functionality plus gesture support, spring animations, and snap points. |
| 2025-12-03 | Added Skeleton with pulse and shimmer animations. |
| 2025-12-03 | Began implementation of gesture-aware Sheet/Drawer interactions. |
| 2025-12-02 | Added Separator with orientation, color, and opacity support. |
| 2025-01-XX | Added ComboBox with virtualization, async loading, and an MD3 outlined variant. |
| 2025-12-02 | Added the expanded Slider API with MD3 animations, range support, and three-state icon functionality. |
| 2025-12-02 | Added Popover component with size variants and arrow support |
| 2025-12-02 | Added Grid layout component with GridItem sub-component |
| 2025-12-02 | Added Container and Flex layout components |
| 2025-12-01 | Updated roadmap to reflect implementation of Textarea, Card, Dialog, Tabs, Tooltip, Input, and Radio components |
| 2025-11-30 | Initial roadmap created |
